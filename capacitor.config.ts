import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.promiseministry.tascd',
  appName: 'TASCD-APP',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
